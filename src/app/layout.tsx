import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/layout/Header'
import Footer from '@/components/layout/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Evalence - AI-Powered Performance Reviews',
  description: 'Reimagine performance reviews with AI-driven insights and seamless integration',
  keywords: ['performance reviews', 'AI', 'HR tech', 'employee management'],
  openGraph: {
    title: 'Evalence - Transforming Performance Reviews',
    description: 'Automate and enhance employee performance reviews with AI',
    type: 'website',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} bg-white text-gray-900 antialiased`}>
        <div className="flex flex-col min-h-screen">
          <Header />
          <main className="flex-grow">
            {children}
          </main>
          <Footer />
        </div>
      </body>
    </html>
  )
}